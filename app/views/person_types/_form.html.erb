<%= form_for(@person_type) do |f| %>
<% if @person_type.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@person_type.errors.count, "error") %> prohibited this person_type from being saved:</h2>
  <ul>
    <% @person_type.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>
<div class="row">
  <div class="block-gray col-xs-12 col-sm-12 col-md-10 col-lg-10 col-md-offset-1 col-lg-offset-1">
    <div class="form-group">
      <%= f.label :role %><br />
      <%= f.text_field :role, class: 'form-control', placeholder: 'Nombre del Role'  %>
    </div>
    <div class="form-group">
      <%= f.label :category %><br />
      <%= f.text_field :category, class: 'form-control', placeholder: 'Categoria'  %>
    </div>
    <div class="form-group">
      <%= f.label :public %><br />
      <%= f.check_box :public, class: 'form-control', checked: true %>
    </div>
    <div class="actions">
      <%= f.submit class: 'btn btn-primary' %>
    </div>
  </div>
</div>
<% end %>
